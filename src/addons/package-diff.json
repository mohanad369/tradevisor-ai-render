{
  "_note": "ADD these to package.json dependencies (DO NOT replace entire file)",
  "dependencies": {
    "openai": "latest"
  },
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push"
  }
}
